import {
    createTheme
} from '@mui/material';

export const theme = createTheme({
    palette: {
        primary: {
            dark: '#EA4A00',
            main: '#FE5000',
            light: '#FF5D13',
        },
    },
});
